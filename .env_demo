# MONGODB_URI=mongodb://root:RPbMknbcvuqzYVeuTxtd@180.211.96.19:2008/ecrm?authSource=admin
# MONGODB_DB=ecrm

# POSTGRES_HOST=localhost
# POSTGRES_PORT=5433
# POSTGRES_USER=postgres
# POSTGRES_PASSWORD=postgres
# POSTGRES_DB=mongos_sync
# POLL_INTERVAL_MS=5000

# # Primary LLM — debopam Qwen2.5-Coder 3B (fast Text2SQL, ~5s)
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_PRIMARY_MODEL=debopam/Text-to-SQL__Qwen2.5-Coder-3B-FineTuned:latest
# OLLAMA_PRIMARY_TIMEOUT=8

# # Fallback LLM — Arctic Text2SQL 7B (larger, used when primary fails)
# OLLAMA_FALLBACK_MODEL=a-kore/Arctic-Text2SQL-R1-7B:latest
# OLLAMA_FALLBACK_TIMEOUT=20

# # =============================================================================
# # REASONING LLM ROUTING — classify / decompose / synthesize
# # NOTE: Text2SQL models above are NOT changed (fine-tuned, stay on Ollama)
# # =============================================================================
# # Provider chain per task:
# #   classify  → Groq 8b  → Gemini flash-lite → Ollama 1.5b
# #   decompose → Groq 8b  → Gemini flash-lite → OpenRouter → Ollama 7b
# #   synthesize→ Groq 70b → Gemini flash-lite → OpenRouter → Ollama 7b
# #
# # Rate budget at 3-5 queries/min (50% complex):
# #   Groq 8b  classify+decompose : ~2,250 TPM / 6,000 TPM limit  ✓
# #   Groq 70b synthesize         : ~7,500 TPM / 12,000 TPM limit ✓
# # =============================================================================

# # ── Groq (primary — fastest, 14,400 RPD) ─────────────────────────────────────
# GROQ_API_KEY=
# GROQ_CLASSIFY_MODEL=llama-3.1-8b-instant
# GROQ_DECOMPOSE_MODEL=llama-3.1-8b-instant
# GROQ_SYNTHESIS_MODEL=llama-3.3-70b-versatile

# # ── Gemini (secondary — 250K TPM, 1K RPD) ────────────────────────────────────
# GEMINI_API_KEY=
# GEMINI_CLASSIFY_MODEL=gemini-2.5-flash-lite
# GEMINI_DECOMPOSE_MODEL=gemini-2.5-flash-lite
# GEMINI_SYNTHESIS_MODEL=gemini-2.5-flash-lite

# # ── OpenRouter (tertiary — free tier last-resort cloud fallback) ──────────────
# OPENROUTER_API_KEY=
# OR_DECOMPOSE_MODEL=meta-llama/llama-3.1-8b-instruct
# OR_SYNTHESIS_MODEL=meta-llama/llama-3.1-8b-instruct

# # ── Ollama local (final fallback — always available, no API key) ──────────────
# OLLAMA_REASONING_MODEL=qwen2.5:7b
# OLLAMA_REASONING_TIMEOUT=90
# OLLAMA_CLASSIFY_MODEL=qwen2.5:1.5b

# STRICT_GROUNDED_MODE=false
# AGENT_MAX_RETRIES=0

# # Redis semantic cache
# REDIS_HOST=localhost
# REDIS_PORT=6379
# REDIS_DB=0
# REDIS_CACHE_TTL=3600
# CACHE_DISABLED=true
# CACHE_SIM_THRESHOLD=0.70
# EMBED_MODEL=nomic-embed-text:latest
